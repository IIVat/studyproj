package taglessfinal.model

//here can be anything inside
//the class can have huge amount of optional fields
//every field MUST BE optional
final case class ExtraData(esCode: Option[String], incrementId: Option[Int])

